class DomDoubleDirBorderScaler extends DomBorderScaler {
  
  constructor(parentElement = null) {

    super(parentElement)
   
  }

  init(x, y, w, h) {

    super.init(x, y, w, h)
    
    
  }
  
}