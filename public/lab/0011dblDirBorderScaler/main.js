let init = (parentHtmlElement) => {

  let myDomElement = new DomWindow(parentHtmlElement)
  myDomElement.init(100, 100, 320, 240)

  // let myBorderElem = new DomBorderScaler(parentHtmlElement)
  // myBorderElem.init(200, 100, 320, 50)
  console.log("init")

}